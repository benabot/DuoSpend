interface:
  display_name: "DuoSpend"
  short_description: "Repo-specific patterns and workflows for DuoSpend"
  default_prompt: "Use the DuoSpend repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true